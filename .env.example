# Provider: local | openai | gemini
LLM_PROVIDER=local

# Optional: OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Optional: Gemini (API key from Google AI Studio / Vertex, depending on your setup)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-pro

# Service settings
APP_ENV=dev
